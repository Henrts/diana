{
  "name": "@diana-ui/card",
  "version": "0.0.31",
  "main": "lib/index.js",
  "module": "module/index.js",
  "files": [
    "lib",
    "module"
  ],
  "license": "MIT",
  "scripts": {
    "build": "yarn compile:lib && yarn compile:module",
    "compile:lib": "tsc --target es5 --module es2015 --outDir lib",
    "compile:module": "tsc --target es6 --module esnext --outDir module",
    "clean:files": "rimraf lib module",
    "prepare": "yarn clean:files && yarn build"
  },
  "dependencies": {
    "@diana-ui/base": "^0.1.20",
    "@diana-ui/button": "^0.1.27",
    "@diana-ui/divider": "^0.1.24",
    "@diana-ui/icon": "^0.2.14",
    "@diana-ui/types": "^0.1.14",
    "@diana-ui/typography": "^0.1.20"
  },
  "devDependencies": {
    "@diana-ui/button": "^0.1.13"
  },
  "peerDependencies": {
    "react": "^16.12.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
